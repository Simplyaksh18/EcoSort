Place your final PNG icons here for GitHub Pages / Bubblewrap (APK) packaging.

Required filenames and locations (exact):

- docs/icons/icon-192.png (192x192 PNG)
- docs/icons/icon-512.png (512x512 PNG)

Notes:

- I added SVG placeholders (`icon-192.svg` and `icon-512.svg`) so the site still shows a logo in browsers.
- Bubblewrap / Android requires PNGs; replace the SVGs with proper PNGs at the exact PNG paths above.
- After adding PNGs, commit and push; the APK tooling will find the icons at `/EcoSort/icons/...`.
